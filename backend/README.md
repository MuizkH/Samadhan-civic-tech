# Backend — Phase 2

Not implemented yet.

Phase 1 removed the previous Supabase-based backend from `frontend/` and
replaced it with a temporary local mock data layer
(`frontend/src/shared/mock/`) so the app runs standalone with no
credentials or backend of any kind.

This directory is a placeholder for the Phase 2 backend. See
[`docs/roadmap.md`](../docs/roadmap.md) for what it needs to provide, and
[`docs/phase-1-supabase-removal.md`](../docs/phase-1-supabase-removal.md)
for the exact contract the frontend currently expects from each mock
adapter.
