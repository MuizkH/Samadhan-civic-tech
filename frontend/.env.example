# Copy this file to .env and fill in real values. Never commit .env.
# All values below are obviously fake — replace every one before starting the app.

# ---------------------------------------------------------------------------
# Backend API URL
# The full base URL of the running backend (no trailing slash).
# In development: http://localhost:4000
# In production: your deployed backend URL (e.g. https://api.your-domain.example.com)
# ---------------------------------------------------------------------------
VITE_API_BASE_URL=http://localhost:4000
